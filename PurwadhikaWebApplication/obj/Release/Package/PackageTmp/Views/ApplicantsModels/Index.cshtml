@model IEnumerable<PurwadhikaWebApplication.Models.ApplicantsModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Name
        </th>
        <th>
            Batch
        </th>
        <th>
            Applying For
            @*@Html.DisplayNameFor(model => model.Students.Firstname)*@
            
        </th>
        <th>
            Action
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Students.Fullname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Students.Batch)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Jobs.JobName)
            
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
