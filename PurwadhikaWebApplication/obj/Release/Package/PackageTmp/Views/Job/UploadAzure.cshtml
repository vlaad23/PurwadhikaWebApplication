<h2> Test Upload Image </h2>

@foreach (var item in Model) {
    <img src="@item" width="200" height="200"/>
}

<p>
    @using (Html.BeginForm("UploadAzure", "Job", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        
        <input type="file" name="image"/>
        <input type="submit" value="Upload"/>
    }
</p>

@*<ul style="list-style-type:none; padding:0;">
    @foreach (var item in Model)
    {
    <li>
        <img src="@item" alt="image here" width="100" height="100" />
        <a id="@item" href="#" onclick="deleteImage('@item');">Delete</a>
     </li>
    }
</ul>*@

@*<img src="@Url.Content(@Model.Image)" alt="image here" />*@ 
 
<script>
    function deleteImage(item) {
        var url = "/Job/DeleteBlob";
        $.post(url, { Name: item }, function (data) {
            window.location.href = "/Job/UploadAzure";
        });
    }
</script>